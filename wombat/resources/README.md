# wombat/resources

Runtime resource files bundled with Wombat (and included in standalone builds).

## `config.event_definitions.yml`

The default event library. The Events panel loads it on startup, and users can
load their own definitions over it.

This file is the `config.event_definitions.yml` from
[funscript-tools](https://github.com/edger477/funscript-tools) by **edger477**,
redistributed here under the MIT License (Copyright © 2024 edger477). Its license
text is preserved alongside it in `config.event_definitions.LICENSE.txt`. If you
update the bundled copy, refresh that license file too.
